# Lead Suppression List (do not re-target / re-demo)

Set 2026-06-18. These are ALREADY-DEMOED historical leads in the **spiritual/healing/energy/hypno** category,
which is EXCLUDE for the current personal-salon (relaxation) pipeline (Yu decision 1). They must NOT be
re-collected, re-demoed, or contacted. Machine list: `LEAD_SUPPRESSION_LIST.csv` (140 rows).

- Fukuoka set: `kimotostudio12.netlify.app/0000`–`/0088` (89 rows; = Fukuoka worklog 260223, already analyzed).
- Hokkaido set: `kimotostudio12.netlify.app/0100`–`/0150` (51 rows).
- Suppression keys: demo-URL lineage (kimotostudio12.netlify.app/<id>) + the lead's domain/normalized place name.
- These are SPIRITUAL/EXCLUDE — they are NOT relaxation-salon discovery seeds.
